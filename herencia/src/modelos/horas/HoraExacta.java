package modelos.horas;
/*
 * A partir de la clase Hora implementar la clase HoraExacta, que incluye en la hora los segundos.
 *  Además de los métodos heredados de Hora, dispondrá de:HoraExacta(hora, minuto, segundo),
 *  que construye un objeto con los datos pasados como parámetros.setSegundo(valor), que asigna, si está
 *  comprendido entre 0 y 59, el valor indicado a los segundos.inc(), que incrementa la hora en un segundo
 */

/*
 * Añadir a la clase HoraExacta un metodo que compare si dos horas (la invocante y otra pa-sada como parámetro de
 *  entrada al metodo) son iguales o distintas.
 */
public class HoraExacta {
}
